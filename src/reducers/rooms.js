const rooms = (state = null, action) => {
    switch (action.type) {
        // case 'TEST_THE_THING':
        //     return {
        //         ...state,
        //         test1: !tests.test1
        //     }
        default:
            return state
    }
}
export default rooms